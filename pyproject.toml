[project]
name = "claude-agent-sdk-playground"
version = "0.1.0"
description = "Playground for experimenting with the Claude Agent SDK"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "anyio",
]

[project.optional-dependencies]
dev = [
    "python-dotenv",
]
